# Notes to self & issues

- Current crashing issue: index is out of range when it attempts to load recipes, this is presumably because the private function loadData() in the RecipeViewModel isn't running at any point
